diff --git a/include/aspl/Plugin.hpp b/include/aspl/Plugin.hpp
index 0d27942..d6239c4 100644
--- a/include/aspl/Plugin.hpp
+++ b/include/aspl/Plugin.hpp
@@ -106,11 +106,16 @@ public:
 
     //! Add device to the plugin.
     //! Adds device to the owned object list.
-    void AddDevice(std::shared_ptr<Device> device);
+    //! @param notifyPropertiesChanged Whether to notify HAL that the properties have
+    //! changed.
+    void AddDevice(std::shared_ptr<Device> device, bool notifyPropertiesChanged = true);
 
     //! Remove device from the plugin.
     //! Removes device from the owned object list.
-    void RemoveDevice(std::shared_ptr<Device> device);
+    //! @param notifyPropertiesChanged Whether to notify HAL that the properties have
+    //! changed.
+    void RemoveDevice(std::shared_ptr<Device> device,
+        bool notifyPropertiesChanged = true);
 
     //! @}
 
diff --git a/src/Plugin.cpp b/src/Plugin.cpp
index e40cb42..62964cf 100644
--- a/src/Plugin.cpp
+++ b/src/Plugin.cpp
@@ -87,7 +87,7 @@ bool Plugin::HasDevice(std::shared_ptr<Device> device) const
     return deviceByID.count(device->GetID());
 }
 
-void Plugin::AddDevice(std::shared_ptr<Device> device)
+void Plugin::AddDevice(std::shared_ptr<Device> device, bool notifyPropertiesChanged)
 {
     std::lock_guard writeLock(writeMutex_);
 
@@ -144,14 +144,16 @@ void Plugin::AddDevice(std::shared_ptr<Device> device)
 
     device->RequestOwnershipChange(this, true);
 
-    NotifyPropertiesChanged(
-        {kAudioObjectPropertyOwnedObjects, kAudioPlugInPropertyDeviceList});
+    if (notifyPropertiesChanged) {
+        NotifyPropertiesChanged(
+            {kAudioObjectPropertyOwnedObjects, kAudioPlugInPropertyDeviceList});
+    }
 
 end:
     GetContext()->Tracer->OperationEnd(op, kAudioHardwareNoError);
 }
 
-void Plugin::RemoveDevice(std::shared_ptr<Device> device)
+void Plugin::RemoveDevice(std::shared_ptr<Device> device, bool notifyPropertiesChanged)
 {
     std::lock_guard writeLock(writeMutex_);
 
@@ -201,8 +203,10 @@ void Plugin::RemoveDevice(std::shared_ptr<Device> device)
 
     device->RequestOwnershipChange(this, false);
 
-    NotifyPropertiesChanged(
-        {kAudioObjectPropertyOwnedObjects, kAudioPlugInPropertyDeviceList});
+    if (notifyPropertiesChanged) {
+        NotifyPropertiesChanged(
+            {kAudioObjectPropertyOwnedObjects, kAudioPlugInPropertyDeviceList});
+    }
 
 end:
     GetContext()->Tracer->OperationEnd(op, kAudioHardwareNoError);
